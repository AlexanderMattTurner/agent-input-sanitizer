- Release automation: the post-merge `tag-release` workflow is now `version-update` and publishes the GitHub Release without a separate tag-push step—`gh release create` creates the tag at the released commit.
